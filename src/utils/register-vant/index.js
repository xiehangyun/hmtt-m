import Vue from 'vue'
import { Button,
  Cell,
  CellGroup,
  Icon,
  NavBar,
  Field,
  Toast,
  CountDown } from 'vant'

Vue.use(Button)
  .use(Cell)
  .use(Icon)
  .use(NavBar)
  .use(Field)
  .use(CellGroup)
  .use(Toast)
  .use(CountDown)
